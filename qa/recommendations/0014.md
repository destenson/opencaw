# QA Loop 0014 Recommendations

Sessions analyzed: 1 session (083853), 1 user question asked, 2 prompt files total (classification + answer). Model: Qwen3.6-35B-A3B-UD-Q2_K_XL.

**Critical context:** This loop produced almost no usable data. Only one question was asked before the session aborted. The findings below are primarily about why the session failed to run, not about retrieval quality — because retrieval quality was never exercised.

---

## 1. `truncate_at_chat_boundary` silently leaves trailing `<|im_end|>` tokens in text, triggering false degenerate detection (critical, new)

**Evidence:** Both prompt files log "Error: degenerate output from Qwen3.6-35B-A3B-UD-Q2_K_XL: `<think>\n\n</think>\n\nBased on the recalled context, **OpenCAW** stands for **Context as Workspace**...`". The sample is clearly not degenerate — it is a valid, on-topic response. Yet `is_looping()` flagged it.

**Root cause:** `truncate_at_chat_boundary()` in `caw-core/src/lib.rs` strips `<|im_end|>` only when there is content *after* it:

```rust
if let Some(pos) = text.find("<|im_end|>") {
    let after = text[pos + "<|im_end|>".len()..].trim();
    if !after.is_empty() {  // trailing stop token: condition is false → NOT stripped
        return text[..pos].trim_end();
    }
}
```

A properly-stopped Qwen3 model emits `<|im_end|>` as its terminal stop token. With nothing after it, `truncate_at_chat_boundary` leaves it in. `is_looping` then sees it and returns `true` unconditionally (the first `if text.contains("<|im_end|>")` branch in `caw-core/src/lib.rs:90`). The response is flagged as degenerate even though the response text before the stop token is correct.

**Why it matters:** Every Qwen3 response that terminates cleanly triggers this false positive. The error propagates to caw-cli exit code → qa.sh inner loop stops after the first question → session data for loop 0014 is 1 question instead of 9. Fixes applied in loop 0013 (B11 strip, B12 think-block extraction) cannot be verified because the session never runs.

**Recommended fix:** Remove the `if !after.is_empty()` guard for the trailing-stop-token case. Strip `<|im_end|>` unconditionally when it appears:

```rust
if let Some(pos) = text.find("<|im_end|>") {
    return text[..pos].trim_end();
}
```

The original intent — only truncate when the model is generating additional conversation turns — is already covered by the `<|im_start|>` check above it. A bare trailing `<|im_end|>` with no following content is a normal stop token, not a runaway generation.

---

## 2. Session aborted after 1 question — QA dataset provides no retrieval signal (critical, consequence of #1)

**Evidence:** The `.caw0014/` directory contains exactly 2 prompt files and no `session-*.md` files. Previous loops ran 2–3 sessions with 3 questions each. No consolidation notes exist in `index.db` (0 rows in `consolidation_notes`).

**Why it matters:** Fixes for B11 (chat-template token stripping) and B12 (think-block extraction as answer) were applied in loop 0013. Loop 0014 was supposed to confirm they worked. With the session aborting on the first question due to the false-positive degenerate flag, those fixes remain unverified. Any regression introduced while fixing B11/B12 — such as the `truncate_at_chat_boundary` change — would also go undetected.

**Recommended action:** Fix #1 first. After that fix, re-run the QA loop and verify that sessions complete all 3 questions.

---

## 3. Search-candidates mode injects file-list metadata but no content — model answers from titles alone (high, ongoing)

**Evidence:** Turn 2 injects 573 tokens labeled `[fragment: search-candidates | 573 tokens]`. The fragment is a list of 10 matching files with only their chunk titles and one-line previews. README.md chunk 1 ("contains OpenCAW - Context as Workspace, Architecture, Crates, Anthropic") was the top candidate but its content was not loaded. The model answered "OpenCAW stands for Context as Workspace" — barely more than what the candidate list itself says.

**Why it matters:** B4 is still active: the system prompt tells the model to request file loads, but the QA harness has no mechanism to fulfill such requests. For a query like "what is opencaw and why should I use it?", the README's first chunk is the canonical answer source and should be loaded, not just listed. The model cannot answer *why* to use it from a file title.

**Recommended action:** For overview queries that match the README, SCOPE.md, or context-as-workspace.md, load the first chunk directly instead of showing it as a candidate. The search-candidates mode is appropriate for wide-cast retrieval over many files; it is counterproductive when the top candidate is the single most authoritative document for the query.

---

## 4. `codebase-review-report.md` ranks as a top search candidate for user-facing queries (medium, new)

**Evidence:** `codebase-review-report.md` chunk 1/13 was the 4th candidate returned for "what is opencaw and why should I use it?" It appeared alongside README.md, SCOPE.md, and the context-as-workspace design doc — all primary documentation. `codebase-review-report.md` is a generated review artifact from a prior analysis pass, not authoritative user-facing documentation.

**Why it matters:** This is a subtle variant of B3: a generated artifact (the review report) surfaces alongside primary docs and could displace them from the budget if content were loaded. The report's language is descriptive but written as a quality audit, not as an answer to "what is this and why should I use it?". Semantic similarity pulled it in because it discusses the same concepts.

**Recommended action:** Add `codebase-review-report.md` to the retrieval skip list, or add it to a "meta-document" category that is never served as primary context for user queries. Consider a broader rule: files whose names contain `review`, `report`, or `analysis` are audit artifacts, not documentation, and should be excluded unless the query is explicitly about the review.

---

## 5. `CLAUDE.md` surfaces as a search candidate for end-user queries (low, new)

**Evidence:** Chunk 2/2 of `./CLAUDE.md` was in the search candidates for "what is opencaw". That chunk contains instructions like "NEVER HARDCODE RESPONSES" — process guidance for AI assistants working on the codebase, not documentation for users of the library.

**Why it matters:** `CLAUDE.md` is a development-process file. Its retrieval for user-facing queries adds noise and could displace actual documentation. A user asking "what is opencaw" does not benefit from seeing constraints on how the AI should implement features.

**Recommended action:** Add `CLAUDE.md` (and any `.claude/` directory contents) to the retrieval skip list. These are instructions for the coding assistant, not workspace documentation. The `should_skip` mechanism already handles `scripts/` and `.caw*` — adding `CLAUDE.md` is a one-line extension.

---

## 6. No consolidation notes generated in 0014 — eviction never triggered (informational)

**Evidence:** `SELECT COUNT(*) FROM consolidation_notes` returns 0 in the 0014 index. With only 1 question asked before session abort, no eviction cycle fired.

**Why it matters:** This is not a new bug — it is a consequence of #1. It confirms that the consolidation pipeline has had no opportunity to run in this loop. The zero-note observation from prior loops (which had actual eviction cycles) remains the more concerning signal; this loop simply had no eviction because the session never ran long enough.

**No action required beyond fixing #1.**
