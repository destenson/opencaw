# QA Loop 0013 Recommendations

Sessions analyzed: 3 sessions (082208, 082332, 082520), 2–3 turns logged per session (14 prompts in session 082520 despite only 3 turns in the log), model Qwen3.6-35B-A3B-UD-Q2_K_XL.
Index: same corpus as 0012.

---

## 1. Qwen3 chat-template tokens are leaking into session history and re-injected verbatim (critical, new)

**Evidence:** In session 082520 turn-8 prompt, the content after the `─────────────────────────` delimiter (normally the model's response) contains raw Qwen3 chat-template tokens: `<|im_end|>`, `<|im_start|>user`, `<|im_start|>assistant`, and `</think>`. The prior assistant response to "is opencaw development completed?" appears in the prompt **four times** verbatim — once in each of the two session-history fragments and twice more as part of the appended raw model output. Grep confirms 4 occurrences of the response's first sentence in that single prompt file.

**Root cause:** The adapter is returning the model's raw completion including its chat-template wrapper tokens rather than just the decoded answer text. When that raw output is stored as a session turn and later re-injected as history, the session-history fragment carries `<|im_end|><|im_start|>user...<|im_start|>assistant</think>` inline. The model then sees what looks like a second continuation of the same conversation replayed inside the context window, which triggers it to generate the same answer again.

**Why it matters:** This is distinct from the B7 echo-chamber (prior response outcompeting workspace stubs by score). Here the same response literally appears multiple times as literal text in one prompt. Combined with B7, a single turn in session 3 had the "is opencaw development completed?" answer present ≥4 times, consuming ~3000 tokens and leaving little room for workspace stubs. It also corrupts anything downstream that parses the session log — the log contains raw Qwen3 special tokens that are not valid Markdown.

**Recommended action:** Strip chat-template tokens (`<|im_end|>`, `<|im_start|>...`, `<think>...</think>` blocks) from model output before storing the response in the session turn. The Qwen3 adapter should extract only the final assistant answer text, not the raw completion string.

---

## 2. Session log truncation bug still active: 14 prompts, 3 turns logged

**Evidence:** Session 082520 has 14 prompt files but the session markdown (`session-20260508-082520.md`) contains only 3 turns. The remaining 11 prompts (turns 4–14) were silently discarded.

**Why it matters:** QA analysis of this session is based on a 21% sample. Any issues occurring after turn 3 are invisible. Combined with observation 1, the session log is also corrupted by raw model tokens — making automated analysis unreliable.

**Recommended action:** Flush the session log after every turn, not only at session end. This is already listed in TODO.md as an open item. It has now been confirmed active in every QA loop since 0003.

---

## 3. B7/B10 echo chamber is still the dominant retrieval failure — amplified by observation 1

**Evidence:** Session 082520 turn-3 prompt shows 3 session-history fragments totaling 2013 tokens (1165 + 818 + 30). For a query about caw-curation ("what can we do to improve on the caw-curation crate?"), the injected context contained zero caw-curation source stubs — identical failure to QA 0012 observation 2. The model answered from BUGS.md references only, correctly noting B9 and B10 but not having any `caw-curation/src/` fragments to reason about.

The `session_history_budget_fraction = 0.25` cap from QA 0012 is a meaningful improvement — session history no longer consumes 58% of the budget. But three concurrent session-history fragments still occupy a substantial slice, and the Qwen3 token leakage (observation 1) re-introduces the verbatim repetition problem at a worse scale.

**Why it matters:** The model consistently answers from prior session responses rather than from workspace stubs. For stable queries like "what is opencaw?", this produces plausible answers. For specific queries like "what can we do to improve caw-curation?", it produces hallucinated recommendations derived from prior sessions rather than from the actual code.

**Recommended action:** The budget fraction cap is necessary but not sufficient. The underlying problem — model responses outscoring workspace stubs semantically — requires either (a) excluding model response text entirely from the in-memory embedding pool (store only user queries for session continuity), or (b) applying a hard score ceiling to session-history fragments so they can never outrank the top workspace stub for the current query.

---

## 4. TODO.md inventory queries still fail: only 1 chunk retrieved for count questions

**Evidence:** Session 082332 turn 3 ("how many todos are left?") produced a model response explicitly admitting "I cannot provide an exact count" — only chunk 20/23 of TODO.md was retrieved. Session 082208 has no inventory count query, but the intent classifier correctly classified "how many todos are left?" as `is_inventory_request: true` (confirmed in intent bench prompt output).

**Why it matters:** The intent classification is working. The augmentation signals are being produced. They are not being acted upon. `is_inventory_request = true` should trigger proactive loading of all chunks of relevant documents (TODO.md, BUGS.md, SCOPE.md) up to the token budget — but this "intent-driven proactive context injection" is listed in TODO.md as explicitly unimplemented.

**Recommended action:** Implement the augmentation-signal-to-retrieval wiring: when `is_inventory_request` or `is_status_request` is set, and the top-k hits are fragments of the same file, load all remaining chunks of that file before the answer model runs. This is the single highest-value retrieval improvement for status/count queries.

---

## 5. caw-bench stubs still surfacing for non-benchmark overview queries

**Evidence:** In session 082208 turn 1 ("what is opencaw and why should I use it?"), the consolidation notes show that `intent_bench.rs` chunk 7/13, `tune.rs` chunk 5/16, `sysdoc.rs`, and `niah_corpus.rs` were all retrieved and then evicted. The 0.5× penalty from QA 0010 is reducing their final rank but not preventing admission. The same stubs appear in the top-30 by token_estimate in the database.

**Why it matters:** These stubs consume retrieval slots and then get evicted almost immediately — they're noise admissions that burn budget. The penalty suppresses them partially but the threshold between "admitted and evicted" and "not admitted at all" is too fine.

**Recommended action:** Either raise the caw-bench penalty multiplier for non-benchmark queries (try 0.25× instead of 0.5×), or add `caw-bench/src/bin/` to the retrieval skip list for sessions that aren't explicitly benchmarking. The harness-implementation files (`build_index.rs`, `tune.rs`, `intent_bench.rs`) have no value as context for users asking architectural overview questions.

---

## 6. Consolidation notes carry no synthesized cross-session signal

**Evidence:** All 173 consolidation notes in the index follow the mechanical format: "Evicted (relevance decayed to X) during query about '...'." For stubs that have been evicted 3+ times across sessions (e.g., `probe_recall.rs`, `caw-adapters/src/ollama.rs`), the notes contain 3 separate eviction records, each with the same structure. Nothing is synthesized: there is no note that says "this stub has been evicted from every session on general overview questions; it is unlikely to be relevant to architectural queries."

**Why it matters:** The consolidation note is the only cross-session learning signal. If it only records raw eviction events without synthesis, the retriever cannot use it to lower the prior probability of re-admission for chronically irrelevant stubs. The cap of 2 displayed notes (plus "N older omitted") preserves the display budget but loses the historical signal entirely for stubs with many evictions.

**Recommended action:** Add a per-stub eviction-count field (or derive it from consolidation notes count). After N consecutive evictions with no positive engagement, synthesize a note like "Chronic no-engagement: evicted N times without annotation or probe match across sessions covering [topic list]. Low prior for overview queries." This directly supports the "chronic no-engagement stubs" TODO item.

---

## 7. Model output format signals confusion about its interactive capabilities

**Evidence:** In sessions 082332 and 082520, the model consistently ends multi-turn answers with phrases like "Would you like me to load the full `./SCOPE.md`?" or "Would you like me to load `./TODO.md` or `./codebase-review-report.md` to get more specific details?" These appear in 6+ responses across both sessions.

**Why it matters:** The model has no mechanism to dynamically load documents on request — it cannot execute tools in this context. These offers are hollow: they mimic an interactive assistant that can respond to follow-up commands, but in the QA harness the next turn is a pre-scripted question, not a response to the model's offer. The phrasing suggests the model has learned (from its training or from session history) that context-loading is a desirable behavior, but it's producing it as output prose rather than as a retrieval signal.

**Recommended action:** Two options: (a) add a system prompt clause explicitly stating "You cannot request additional context loading — use only the provided fragments," or (b) treat model output containing "Would you like me to load..." as a signal that the current context is insufficient and trigger proactive additional retrieval for the next turn. Option (b) is more interesting architecturally but requires parsing model output. Option (a) is a quick noise reduction.

---

## 8. stub token_estimate values are structurally misleading the scheduler

**Evidence:** The database shows 19 stubs with token_estimate between 460–498 (near the apparent cap). In the actual prompts, these stubs inject 28–280 tokens of content. `caw-bench/src/sysdoc.rs` is estimated at 491 tokens but injects 55 tokens. `caw-core/src/tokenizer.rs` chunk 2/3 is estimated at 496 tokens but injects ~140 tokens. The discrepancy is systematic and large.

**Why it matters:** The greedy scheduler reserves budget based on `token_estimate`. If a stub is estimated at 490 tokens but only injects 55 tokens, the scheduler effectively wastes 435 tokens of budget headroom on that slot. This means the workspace reaches its token ceiling prematurely, leaving out stubs that would actually fit. The problem compounds across 40+ fragments per prompt.

**Recommended action:** After each fragment injection, log the actual injected token count vs the estimated count (this is the QA 0009 recommendation 10 that remains open). Add a per-session aggregate alert when total overestimation exceeds, say, 20% of the budget. Once the scale of the problem is instrumented, update token_estimate at index-build time using the same tokenizer used at injection time, not a pre-injection estimate.

---

## 9. Session 082520 turn 4 response begins with `<think>` (thinking trace not stripped)

**Evidence:** The grep output for session 082520 prompts shows `Response: <think>` for turn 4. This means the model's extended-thinking prefix `<think>...</think>` was not stripped before the response was stored, and at least one turn's session log entry begins with the raw reasoning rather than the final answer.

**Why it matters:** Session log entries beginning with `<think>` will produce misleading context when re-injected as history — the model will see its own prior reasoning as if it were an answer. It also corrupts the session log markdown. This may be related to observation 1 (chat template token leakage) or may be a separate Qwen3-specific issue where the adapter is not discarding the thinking section.

**Recommended action:** Strip `<think>...</think>` blocks from Qwen3 responses before returning `CompletionResponse.answer`. The thinking trace has value for diagnostics but should not be the stored answer text.

---

## 10. Cross-session echo is confirmed and measurable: session 2 and session 3 produce near-identical outputs

**Evidence:** Session 082332 and session 082520 ask the same three questions in the same order. Session 082520 turn 2 and turn 3 answers are near-verbatim duplicates of session 082332 turns 2 and 3. The phrasing, structure, and recommended action points are identical even down to "Start `caw-bench-intent` development" and "Update `caw-server` docs." Session 082520 turn 3 additionally imported the full session 082332 assistant response into the history fragments (confirmed by the session-history token counts and the 4× repetition count).

**Why it matters:** This is the B7 echo in its clearest form: session 3 didn't retrieve from the workspace, it retrieved from session 2's model output. The model's answers to "what's next to work on?" are derived from what it said in the prior session, not from what TODO.md and SCOPE.md currently say. Any work done between sessions (new commits, updated TODOs) would be invisible.

**Recommended action:** Same as observation 3. Additionally, consider running sessions with a forced cold start (no session history injection) as a control to quantify how much the echo is distorting answers vs. ground truth.
